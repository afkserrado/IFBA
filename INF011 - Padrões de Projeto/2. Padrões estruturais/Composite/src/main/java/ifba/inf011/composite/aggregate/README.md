# Composite agregador

* Interface/abstração comum (`Equipment`);
* Estrutura hierárquica;
* Operações agregadas (`power()` e `netPrice()`);
* Composite combina resultados dos filhos;
* Estrutura ordenada (`List`);
* Sem cache;
* Sem parent;
* Sem restrição de tipos além do próprio tipo `Equipment`.
